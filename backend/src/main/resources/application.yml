server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/2023011345?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

app:
  jwt:
    secret: campus-ershou-market-jwt-secret-key-2026-springboot
    expire-hours: 72
  upload:
    path: ./uploads/
  captcha:
  order:
    auto-confirm-days: 7
    return-hours: 24
